name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '29 14 * * 1'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [ "javascript-typescript" ]
        build_mode: [ "none" ]      # <-- JS/TS must be "none"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          # Or read from .nvmrc: node-version-file: '.nvmrc'
          node-version: '20'

      # If you use npm. Swap for yarn/pnpm as needed.
      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            n
